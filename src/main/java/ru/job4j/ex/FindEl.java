package ru.job4j.ex;

public class FindEl {
    public static int indexOf(String[] value, String key) { //метод поиска индекса элемента в строковом массиве.
        int rsl = -1;
          for ( ? : ?) {

          }
        return rsl;
    }

    public static void main(String[] args) {
        try {
            indexOf(" ", " ");
        } catch (ElementNotFoundException e) {
            e.printStackTrace();
        }
    }

}
